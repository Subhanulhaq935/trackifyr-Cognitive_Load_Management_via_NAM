'use strict'

/** Same numeric set as `lib/activityMetrics.js` (Fusion + desktop). Keep in sync — parity test enforces equality. */
module.exports = {
  ACTIVITY_SCALE_MIN: 0,
  ACTIVITY_SCALE_MAX: 100,
  ACTIVITY_HIGH_THRESHOLD: 70,
  ACTIVITY_LOW_THRESHOLD: 40,
  ACTIVITY_VERY_HIGH_MIN: 80,
  ACTIVITY_VERY_LOW_MAX: 15,
}
